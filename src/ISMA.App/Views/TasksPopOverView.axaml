<UserControl xmlns="https://github.com/avaloniaui"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:vm="using:ISMA.ViewModels.ViewModels"
             x:Class="ISMA.App.Views.TasksPopOverView"
             x:CompileBindings="True"
             x:DataType="vm:TasksPopOverViewModel">

    <Border CornerRadius="6"
            Padding="12"
            MinWidth="340"
            MaxWidth="420"
            Background="{DynamicResource SystemControlBackgroundChromeMediumHighBrush}"
            BorderBrush="{DynamicResource SystemControlForegroundBaseMediumLowBrush}"
            BorderThickness="1"
            BoxShadow="0 4 16 0 #80000000">

        <StackPanel Spacing="12">

            <!-- In Progress Section -->
            <StackPanel>
                <TextBlock Text="In Progress"
                           FontWeight="Bold"
                           FontSize="14"
                           Margin="0,0,0,6" />

                <ItemsControl ItemsSource="{Binding InProgress}"
                              MaxHeight="180">
                    <ItemsControl.ItemTemplate>
                        <DataTemplate x:DataType="vm:InProgressSimulationViewModel">
                            <Border CornerRadius="4"
                                    Padding="8"
                                    Margin="0,2"
                                    Background="{DynamicResource SystemControlBackgroundChromeMediumBrush}"
                                    BorderBrush="{DynamicResource SystemControlForegroundBaseMediumLowBrush}"
                                    BorderThickness="1">
                                <StackPanel Spacing="4">
                                    <TextBlock Text="{Binding ModelName}"
                                               FontWeight="SemiBold"
                                               FontSize="12"
                                               TextTrimming="CharacterEllipsis" />
                                    <ProgressBar Value="{Binding Progress}"
                                                 Minimum="0"
                                                 Maximum="100"
                                                 Height="6"
                                                 Margin="0,2" />
                                    <Button Content="Abort"
                                            Padding="12,3"
                                            FontSize="11"
                                            HorizontalAlignment="Right"
                                            Classes="accent"
                                            Command="{Binding AbortCommand}"
                                            IsEnabled="{Binding CanAbort}" />
                                </StackPanel>
                            </Border>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>
            </StackPanel>

            <!-- Completed Section -->
            <StackPanel>
                <TextBlock Text="Completed"
                           FontWeight="Bold"
                           FontSize="14"
                           Margin="0,0,0,6" />

                <ItemsControl ItemsSource="{Binding Completed}"
                              MaxHeight="160">
                    <ItemsControl.ItemTemplate>
                        <DataTemplate x:DataType="vm:CompletedSimulationViewModel">
                            <Border CornerRadius="4"
                                    Padding="8"
                                    Margin="0,2"
                                    Background="{DynamicResource SystemControlBackgroundChromeMediumBrush}"
                                    BorderBrush="{DynamicResource SystemControlForegroundBaseMediumLowBrush}"
                                    BorderThickness="1">
                                <Grid ColumnDefinitions="*,Auto,Auto,Auto,Auto">
                                    <TextBlock Grid.Column="0"
                                               Text="{Binding ModelName}"
                                               FontWeight="SemiBold"
                                               FontSize="12"
                                               TextTrimming="CharacterEllipsis"
                                               VerticalAlignment="Center" />

                                    <Button Grid.Column="1"
                                            Content="Show"
                                            Padding="8,3"
                                            FontSize="10"
                                            Margin="6,0,0,0"
                                            Command="{Binding ShowCommand}"
                                            VerticalAlignment="Center" />

                                    <Button Grid.Column="2"
                                            Content="Export"
                                            Padding="8,3"
                                            FontSize="10"
                                            Margin="4,0,0,0"
                                            Command="{Binding ExportCommand}"
                                            VerticalAlignment="Center" />

                                    <Button Grid.Column="3"
                                            Content="Remove"
                                            Padding="8,3"
                                            FontSize="10"
                                            Margin="4,0,0,0"
                                            Classes="danger"
                                            Command="{Binding RemoveCommand}"
                                            VerticalAlignment="Center" />

                                    <Button Grid.Column="4"
                                            Content="⋯"
                                            FontSize="16"
                                            FontWeight="Bold"
                                            Margin="8,0,0,0"
                                            Padding="4,0"
                                            VerticalAlignment="Center"
                                            Classes="chevron"
                                            IsHitTestVisible="True">
                                        <Button.Flyout>
                                            <Flyout>
                                                <Border CornerRadius="4"
                                                        Padding="10"
                                                        MinWidth="220"
                                                        Background="{DynamicResource SystemControlBackgroundChromeMediumHighBrush}"
                                                        BorderBrush="{DynamicResource SystemControlForegroundBaseMediumLowBrush}"
                                                        BorderThickness="1"
                                                        BoxShadow="0 2 8 0 #60000000">
                                                    <StackPanel Spacing="6">
                                                        <TextBlock Text="{Binding ModelName}"
                                                                   FontWeight="SemiBold"
                                                                   FontSize="12" />
                                                        
                                                        <TextBlock Text="Cauchy Initials"
                                                                   FontWeight="SemiBold"
                                                                   FontSize="10"
                                                                   Margin="0,4,0,2"
                                                                   Foreground="{DynamicResource SystemControlForegroundBaseHighBrush}"/>
                                                        <StackPanel Orientation="Horizontal" Spacing="8">
                                                            <TextBlock Text="Start:" FontSize="10"/>
                                                            <TextBlock Text="{Binding Parameters.CauchyInitials.StartTime, StringFormat='{}{0:F2}'}" FontSize="10"/>
                                                        </StackPanel>
                                                        <StackPanel Orientation="Horizontal" Spacing="8">
                                                            <TextBlock Text="End:" FontSize="10"/>
                                                            <TextBlock Text="{Binding Parameters.CauchyInitials.EndTime, StringFormat='{}{0:F2}'}" FontSize="10"/>
                                                        </StackPanel>
                                                        <StackPanel Orientation="Horizontal" Spacing="8">
                                                            <TextBlock Text="Step:" FontSize="10"/>
                                                            <TextBlock Text="{Binding Parameters.CauchyInitials.InitialStep, StringFormat='{}{0:F3}'}" FontSize="10"/>
                                                        </StackPanel>
                                                        
                                                        <TextBlock Text="Integration Method"
                                                                   FontWeight="SemiBold"
                                                                   FontSize="10"
                                                                   Margin="0,4,0,2"
                                                                   Foreground="{DynamicResource SystemControlForegroundBaseHighBrush}"/>
                                                        <StackPanel Orientation="Horizontal" Spacing="8">
                                                            <TextBlock Text="Method:" FontSize="10"/>
                                                            <TextBlock Text="{Binding Parameters.IntegrationMethod.SelectedMethod}" FontSize="10"/>
                                                        </StackPanel>
<StackPanel Orientation="Horizontal" Spacing="8">
                                                            <TextBlock Text="Accuracy:" FontSize="10"/>
                                                            <TextBlock Text="{Binding Parameters.IntegrationMethod.Accuracy, StringFormat='{}{0:F3}'}" FontSize="10"/>
                                                        </StackPanel>
                                                        <StackPanel Orientation="Horizontal" Spacing="8">
                                                            <TextBlock Text="Stable:" FontSize="10"/>
                                                            <TextBlock Text="{Binding Parameters.IntegrationMethod.IsStableInUse}" FontSize="10"/>
                                                        </StackPanel>
                                                    </StackPanel>
                                                </Border>
                                            </Flyout>
                                        </Button.Flyout>
                                    </Button>
                                </Grid>
                            </Border>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>
            </StackPanel>

        </StackPanel>
    </Border>
</UserControl>
